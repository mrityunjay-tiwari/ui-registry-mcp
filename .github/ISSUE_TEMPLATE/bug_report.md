---
name: Bug report
about: Something isn't working the way it should
title: ""
labels: bug
---

**What happened**
A clear description of the bug.

**What you expected**

**Which tool / registry**
e.g. `get_component` on `aceternity`, or `search_components` with query "…".

**To reproduce**
The tool call (arguments) or the prompt you gave the agent.

**Environment**
- MCP client (Claude Code / Cursor / …):
- `ui-registry-mcp` version (`npm view ui-registry-mcp version` or your pin):
- Node version:

**Anything else**
Error output, logs, etc.
